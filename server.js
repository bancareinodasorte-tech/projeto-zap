const express=require('express');
const path=require('path');
const crypto=require('crypto');
const pino=require('pino');
const QRCode=require('qrcode');
const {default:makeWASocket,DisconnectReason,fetchLatestBaileysVersion,initAuthCreds,BufferJSON,proto}=require('@whiskeysockets/baileys');

const app=express();
app.use(express.json({limit:'20mb'}));
app.use(express.static(__dirname,{extensions:['html']}));
const PORT=process.env.PORT||3000;
const SUPABASE_URL=(process.env.SUPABASE_URL||'').replace(/\/+$/,'');
const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||'';
function needDb(){if(!SUPABASE_URL||!KEY)throw new Error('Configure SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY no Render.');}
async function sb(p,opt={}){needDb();const r=await fetch(SUPABASE_URL+p,{...opt,headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json',...(opt.headers||{})}});const t=await r.text();let d=null;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok)throw new Error(d?.message||d?.details||`Supabase ${r.status}`);return d}
const digits=v=>String(v||'').replace(/\D/g,'');
function phone(v){let n=digits(v);if(n.startsWith('00'))n=n.slice(2);if(!n.startsWith('55')&&(n.length===10||n.length===11))n='55'+n;return n}
const validPhone=n=>/^55\d{10,11}$/.test(phone(n));
const now=()=>new Date().toISOString();
const q=s=>encodeURIComponent(s);
function orderCode(){return 'RDS-'+Date.now().toString(36).toUpperCase().slice(-6)+'-'+crypto.randomInt(100,999)}
async function settings(){const x=await sb('/rest/v1/rds_settings?id=eq.main&select=*&limit=1');return x?.[0]||{}}
async function event(type,data={}){try{await sb('/rest/v1/rds_events',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({event_type:type,phone:data.phone||null,campaign_id:data.campaign_id||null,order_id:data.order_id||null,payload:data.payload||{}})})}catch{}}
async function findContact(n){const rows=await sb(`/rest/v1/rds_contacts?phone=eq.${q(phone(n))}&select=*&limit=1`);return rows?.[0]||null}
async function ensureContact(n){let c=await findContact(n);if(c)return c;const p=phone(n);const rows=await sb('/rest/v1/rds_contacts',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({name:`WhatsApp ${p.slice(-4)}`,phone:p,group_name:'ENTRADA WHATSAPP',status:'ATIVO',notes:'Criado automaticamente por mensagem recebida.',source:'WHATSAPP'})});return rows?.[0]}
async function activeRecipient(n){const rows=await sb(`/rest/v1/rds_campaign_recipients?phone=eq.${q(phone(n))}&recipient_status=in.(ATIVO,ENVIADO)&select=*&order=created_at.desc&limit=1`);return rows?.[0]||null}
async function activeOrder(n){const rows=await sb(`/rest/v1/rds_orders?phone=eq.${q(phone(n))}&status=in.(COLETANDO_DADOS,AGUARDANDO_PAGAMENTO,COMPROVANTE_RECEBIDO,PAGAMENTO_CONFIRMADO)&select=*&order=created_at.desc&limit=1`);return rows?.[0]||null}
async function patch(table,id,data){await sb(`/rest/v1/${table}?id=eq.${q(id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({...data,updated_at:now()})})}
async function saveMessage({contact,campaign_id,recipient_id,order_id,phone:ph,id,direction,type,body,mime,file}){try{await sb('/rest/v1/rds_messages',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({contact_id:contact?.id||null,campaign_id:campaign_id||null,recipient_id:recipient_id||null,order_id:order_id||null,phone:phone(ph),wa_message_id:id||null,direction,message_type:type||'text',body:body||null,mime_type:mime||null,file_name:file||null})})}catch{}}

let sock=null,connected=false,starting=false,qrDataUrl='',connectedNumber='',lastError='';
async function authRead(id){const r=await sb(`/rest/v1/zap_auth?id=eq.${q(id)}&select=value&limit=1`);return r?.[0]?JSON.parse(JSON.stringify(r[0].value),BufferJSON.reviver):null}
async function authWrite(id,value){const safe=JSON.parse(JSON.stringify(value,BufferJSON.replacer));await sb('/rest/v1/zap_auth?on_conflict=id',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({id,value:safe,updated_at:now()})})}
async function authDelete(id){await sb(`/rest/v1/zap_auth?id=eq.${q(id)}`,{method:'DELETE'})}
async function authState(){const creds=(await authRead('creds'))||initAuthCreds();return{state:{creds,keys:{get:async(type,ids)=>{const out={};for(const id of ids){let v=await authRead(`${type}-${id}`);if(type==='app-state-sync-key'&&v)v=proto.Message.AppStateSyncKeyData.fromObject(v);out[id]=v}return out},set:async(data)=>{for(const cat of Object.keys(data))for(const id of Object.keys(data[cat]))data[cat][id]?await authWrite(`${cat}-${id}`,data[cat][id]):await authDelete(`${cat}-${id}`)}}},saveCreds:()=>authWrite('creds',creds)}}
async function closeSock(){try{sock?.ws?.close?.()}catch{}sock=null;connected=false;connectedNumber=''}
async function startWA(force=false){if(starting)return;if(sock&&!force)return;starting=true;if(force)await closeSock();try{const {state,saveCreds}=await authState();const {version}=await fetchLatestBaileysVersion();sock=makeWASocket({version,auth:state,logger:pino({level:'silent'}),printQRInTerminal:false,browser:['CANAL DE VENDAS RDS','Chrome','6.0.0'],markOnlineOnConnect:false,syncFullHistory:false,shouldSyncHistoryMessage:()=>false,generateHighQualityLinkPreview:false});sock.ev.on('creds.update',saveCreds);sock.ev.on('connection.update',async u=>{if(u.qr){qrDataUrl=await QRCode.toDataURL(u.qr,{width:420,margin:2});lastError=''}if(u.connection==='open'){connected=true;qrDataUrl='';connectedNumber=digits(sock.user?.id?.split(':')?.[0]||sock.user?.id||'');lastError=''}if(u.connection==='close'){connected=false;const code=u.lastDisconnect?.error?.output?.statusCode||u.lastDisconnect?.error?.statusCode||0;const out=code===DisconnectReason.loggedOut;lastError=String(u.lastDisconnect?.error?.message||'Conexão encerrada');await closeSock();if(!out)setTimeout(()=>startWA(false).catch(()=>{}),3000)}});sock.ev.on('messages.upsert',async({messages,type})=>{if(type!=='notify')return;for(const m of messages||[]){if(!m?.message||m.key?.fromMe)continue;const remote=String(m.key.remoteJid||'');if(remote.endsWith('@g.us')||remote==='status@broadcast')continue;await inbound(digits(remote.split('@')[0]),m).catch(e=>console.error('inbound',e.message))}})}catch(e){lastError=e.message;await closeSock();throw e}finally{starting=false}}
async function sendText(to,text){if(!sock||!connected)throw new Error('WhatsApp não conectado.');const n=phone(to);if(!validPhone(n))throw new Error('Telefone inválido.');const r=await sock.sendMessage(`${n}@s.whatsapp.net`,{text:String(text||'').trim()});return r?.key?.id||null}
async function sendCampaignMessage(to,text,imageData){if(!sock||!connected)throw new Error('WhatsApp não conectado.');const n=phone(to);let r;if(imageData){const m=String(imageData).match(/^data:([^;]+);base64,(.+)$/);if(!m)throw new Error('Imagem inválida.');r=await sock.sendMessage(`${n}@s.whatsapp.net`,{image:Buffer.from(m[2],'base64'),mimetype:m[1],caption:text})}else r=await sock.sendMessage(`${n}@s.whatsapp.net`,{text});return r?.key?.id||null}
function extractMsg(m){const x=m.message||{};if(x.conversation)return{type:'text',body:x.conversation};if(x.extendedTextMessage)return{type:'text',body:x.extendedTextMessage.text||''};if(x.imageMessage)return{type:'image',body:x.imageMessage.caption||'',mime:x.imageMessage.mimetype||'image/jpeg',file:'comprovante-imagem'};if(x.documentMessage)return{type:'document',body:x.documentMessage.caption||'',mime:x.documentMessage.mimetype||'',file:x.documentMessage.fileName||'documento'};return{type:'other',body:''}}
function parseOrderForm(t){const s=String(t||'');const name=(s.match(/nome\s*:\s*([^\n]+)/i)||[])[1]?.trim();const qty=Number((s.match(/quantidade\s*:\s*(\d+)/i)||[])[1]||0);const contact=digits((s.match(/contato\s*:\s*([^\n]+)/i)||[])[1]||'');return{name,qty,contact}}
function wantsBuy(t){return /\b(quero|comprar|compra|bilhete|bilhetes|pedido|reserve|reservar)\b/i.test(String(t||''))||/RDS-COMPRAR/i.test(String(t||''))}
async function cancelFuture(recipientId){if(!recipientId)return;await sb(`/rest/v1/rds_deliveries?recipient_id=eq.${q(recipientId)}&status=eq.AGENDADA`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'CANCELADA',updated_at:now()})});await patch('rds_campaign_recipients',recipientId,{recipient_status:'RESPONDEU',responded_at:now()})}
async function startOrder(ph,contact,recipient,campaignId){const s=await settings();let campaign=null;if(campaignId){const r=await sb(`/rest/v1/rds_campaigns?id=eq.${q(campaignId)}&select=*&limit=1`);campaign=r?.[0]}if(!campaign&&recipient){const r=await sb(`/rest/v1/rds_campaigns?id=eq.${q(recipient.campaign_id)}&select=*&limit=1`);campaign=r?.[0]}const rows=await sb('/rest/v1/rds_orders',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({code:orderCode(),campaign_id:campaign?.id||null,recipient_id:recipient?.id||null,contact_id:contact.id,phone:phone(ph),unit_price:Number(campaign?.unit_price||s.ticket_price||3),status:'COLETANDO_DADOS'})});const o=rows[0];await cancelFuture(recipient?.id);await sendText(ph,`${s.order_prompt}\n\nPedido: ${o.code}`);await event('PEDIDO_INICIADO',{phone:ph,campaign_id:o.campaign_id,order_id:o.id});return o}
async function inbound(ph,m){const msg=extractMsg(m),contact=await ensureContact(ph),recipient=await activeRecipient(ph);let order=await activeOrder(ph);await saveMessage({contact,campaign_id:recipient?.campaign_id,recipient_id:recipient?.id,order_id:order?.id,phone:ph,id:m.key?.id,direction:'IN',type:msg.type,body:msg.body,mime:msg.mime,file:msg.file});if(recipient)await cancelFuture(recipient.id);const s=await settings();if(!s.bot_enabled)return;
  if((msg.type==='image'||msg.type==='document')&&order&&['AGUARDANDO_PAGAMENTO','COLETANDO_DADOS'].includes(order.status)){await patch('rds_orders',order.id,{status:'COMPROVANTE_RECEBIDO',proof_type:msg.type,proof_received_at:now()});await sendText(ph,s.proof_received_message);await event('COMPROVANTE_RECEBIDO',{phone:ph,order_id:order.id,campaign_id:order.campaign_id});return}
  if(order?.status==='COLETANDO_DADOS'){const f=parseOrderForm(msg.body);if(f.name&&f.qty>0){const total=Number(order.unit_price||s.ticket_price||3)*f.qty;await patch('rds_orders',order.id,{customer_name:f.name,quantity:f.qty,contact_phone:f.contact||phone(ph),total_amount:total,status:'AGUARDANDO_PAGAMENTO'});await sendText(ph,`✅ Pedido recebido\nPedido: ${order.code}\nQuantidade: ${f.qty} bilhete(s)\nValor: ${total.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}\n\n💳 PIX: ${s.pix_key}\nFavorecido: ${s.pix_name}\n\nApós pagar, envie o comprovante aqui.`);return}await sendText(ph,`Para continuar o pedido, preencha exatamente assim:\n\nNome:\nQuantidade:\nContato:`);return}
  if(order?.status==='AGUARDANDO_PAGAMENTO'){await sendText(ph,`Seu pedido ${order.code} aguarda pagamento.\nPIX: ${s.pix_key}\nFavorecido: ${s.pix_name}\nDepois envie o comprovante.`);return}
  if(wantsBuy(msg.body)){let campaignId=null;const token=String(msg.body).match(/RDS-COMPRAR:([0-9a-f-]{36})/i);if(token)campaignId=token[1];await startOrder(ph,contact,recipient,campaignId);return}
  const sales=connectedNumber?`https://wa.me/${connectedNumber}?text=${encodeURIComponent('QUERO COMPRAR')}`:'';const office=s.office_whatsapp?`https://wa.me/${phone(s.office_whatsapp)}`:'';await sendText(ph,`${s.unknown_reply}${sales?`\n\n🛒 Compra: ${sales}`:''}${office?`\n🏢 Escritório: ${office}`:''}`)
}

async function createDeliveries(campaign){let contacts;if(campaign.target_mode==='group')contacts=await sb(`/rest/v1/rds_contacts?group_name=eq.${q(campaign.target_group)}&status=eq.ATIVO&select=*`);else contacts=await sb('/rest/v1/rds_contacts?status=eq.ATIVO&select=*');const steps=await sb(`/rest/v1/rds_campaign_steps?campaign_id=eq.${q(campaign.id)}&select=*&order=step_index.asc`);for(const c of contacts||[]){const recs=await sb('/rest/v1/rds_campaign_recipients',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({campaign_id:campaign.id,contact_id:c.id,phone:c.phone,recipient_status:'ATIVO'})});const rec=recs[0];let when=new Date(campaign.start_at);for(const step of steps){if(step.step_index>1)when=new Date(when.getTime()+Number(step.delay_minutes||0)*60000);await sb('/rest/v1/rds_deliveries',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({campaign_id:campaign.id,recipient_id:rec.id,contact_id:c.id,phone:c.phone,step_id:step.id,step_index:step.step_index,scheduled_at:when.toISOString(),status:'AGENDADA'})})}}
}
async function processDue(){if(!connected)return 0;const due=await sb(`/rest/v1/rds_deliveries?status=eq.AGENDADA&scheduled_at=lte.${q(now())}&select=*&order=scheduled_at.asc&limit=50`);let count=0;for(const d of due||[]){try{const rec=(await sb(`/rest/v1/rds_campaign_recipients?id=eq.${q(d.recipient_id)}&select=*&limit=1`))?.[0];if(!rec||rec.recipient_status==='RESPONDEU'){await patch('rds_deliveries',d.id,{status:'CANCELADA'});continue}if(await activeOrder(d.phone)){await patch('rds_deliveries',d.id,{status:'CANCELADA'});continue}const step=(await sb(`/rest/v1/rds_campaign_steps?id=eq.${q(d.step_id)}&select=*&limit=1`))?.[0];let text=step.message||'';if(step.cta_enabled&&connectedNumber){const link=`https://wa.me/${connectedNumber}?text=${encodeURIComponent(`QUERO COMPRAR RDS-COMPRAR:${d.campaign_id}`)}`;text+=`\n\n🛒 *Quero comprar:* ${link}`}const id=await sendCampaignMessage(d.phone,text,step.image_data_url);await patch('rds_deliveries',d.id,{status:'ENVIADA',sent_at:now(),wa_message_id:id,error_text:null});await patch('rds_campaign_recipients',d.recipient_id,{recipient_status:'ENVIADO'});count++;await new Promise(r=>setTimeout(r,1800))}catch(e){await patch('rds_deliveries',d.id,{status:'FALHA',error_text:e.message})}}
  const cids=[...new Set((due||[]).map(x=>x.campaign_id))];for(const id of cids){const pending=await sb(`/rest/v1/rds_deliveries?campaign_id=eq.${q(id)}&status=eq.AGENDADA&select=id&limit=1`);if(!pending.length)await patch('rds_campaigns',id,{status:'FINALIZADA'})}return count}
setInterval(()=>processDue().catch(()=>{}),30000);

app.get('/health',(req,res)=>res.json({ok:true,service:'canal-de-vendas-rds',version:'6.0.0',connected}));
app.get('/api/whatsapp/status',(req,res)=>res.json({ok:true,connected,starting,number:connectedNumber||null,lastError}));
app.post('/api/whatsapp/connect',async(req,res)=>{try{await startWA(Boolean(req.body?.force));res.json({ok:true,message:'Conexão iniciada.'})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/whatsapp/qr',(req,res)=>res.json({ok:true,qrDataUrl,connected}));
app.post('/api/whatsapp/pairing-code',async(req,res)=>{try{const n=phone(req.body.phone);if(!validPhone(n))throw new Error('Telefone inválido.');if(!sock)await startWA(false);if(connected)return res.json({ok:true,message:'WhatsApp já conectado.'});const code=await sock.requestPairingCode(n);res.json({ok:true,code})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/whatsapp/send-test',async(req,res)=>{try{const id=await sendText(req.body.to,req.body.text);res.json({ok:true,id})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/whatsapp/logout',async(req,res)=>{try{if(sock)try{await sock.logout()}catch{}await closeSock();await sb('/rest/v1/zap_auth?id=not.is.null',{method:'DELETE'});res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});

app.get('/api/settings',async(req,res)=>{try{res.json({ok:true,item:await settings()})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/settings',async(req,res)=>{try{await sb('/rest/v1/rds_settings?id=eq.main',{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({...req.body,updated_at:now()})});res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/contacts',async(req,res)=>{try{res.json({items:await sb('/rest/v1/rds_contacts?select=*&order=name.asc')})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/contacts',async(req,res)=>{try{const p=phone(req.body.phone);if(!validPhone(p))throw new Error('WhatsApp inválido.');const data={name:String(req.body.name||'').trim()||`Contato ${p.slice(-4)}`,phone:p,group_name:req.body.group_name||'NOVOS',status:'ATIVO',notes:req.body.notes||'',updated_at:now()};if(req.body.id)await patch('rds_contacts',req.body.id,data);else await sb('/rest/v1/rds_contacts',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({...data,source:'MANUAL'})});res.json({ok:true})}catch(e){res.status(400).json({error:e.message})}});
app.post('/api/contacts/preview-import',async(req,res)=>{try{const existing=await sb('/rest/v1/rds_contacts?select=phone');const set=new Set(existing.map(x=>phone(x.phone)));const seen=new Set();const items=(req.body.items||[]).map(x=>{const p=phone(x.phone),invalid=!validPhone(p),duplicate=set.has(p)||seen.has(p);seen.add(p);return{name:String(x.name||'').trim(),phone:p,group_name:x.group_name||'IMPORTADOS',invalid,duplicate}});res.json({items})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/contacts/import',async(req,res)=>{let saved=0;try{for(const x of req.body.items||[]){const p=phone(x.phone);if(!validPhone(p))continue;try{await sb('/rest/v1/rds_contacts',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({name:x.name||`Contato ${p.slice(-4)}`,phone:p,group_name:x.group_name||'IMPORTADOS',status:'ATIVO',notes:'',source:'IMPORTACAO'})});saved++}catch{}}res.json({ok:true,saved})}catch(e){res.status(500).json({error:e.message})}});

app.get('/api/campaigns',async(req,res)=>{try{const cs=await sb('/rest/v1/rds_campaigns?select=*&order=created_at.desc');for(const c of cs){c.steps=(await sb(`/rest/v1/rds_campaign_steps?campaign_id=eq.${q(c.id)}&select=id`)).length;const ds=await sb(`/rest/v1/rds_deliveries?campaign_id=eq.${q(c.id)}&select=status`);c.total=ds.length;c.sent=ds.filter(x=>x.status==='ENVIADA').length;c.failed=ds.filter(x=>x.status==='FALHA').length;c.cancelled=ds.filter(x=>x.status==='CANCELADA').length}res.json({items:cs})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/campaigns/:id',async(req,res)=>{try{const item=(await sb(`/rest/v1/rds_campaigns?id=eq.${q(req.params.id)}&select=*&limit=1`))[0];const steps=await sb(`/rest/v1/rds_campaign_steps?campaign_id=eq.${q(req.params.id)}&select=*&order=step_index.asc`);res.json({item,steps})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/campaigns',async(req,res)=>{try{if(!req.body.name||!req.body.start_at)throw new Error('Nome e horário são obrigatórios.');if(!Array.isArray(req.body.steps)||!req.body.steps.length)throw new Error('Adicione pelo menos uma mensagem.');const rows=await sb('/rest/v1/rds_campaigns',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({name:req.body.name,unit_price:Number(req.body.unit_price||3),start_at:new Date(req.body.start_at).toISOString(),target_mode:req.body.target_mode||'all',target_group:req.body.target_group||null,status:'RASCUNHO'})});const c=rows[0];for(let i=0;i<req.body.steps.length;i++){const s=req.body.steps[i];await sb('/rest/v1/rds_campaign_steps',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify({campaign_id:c.id,step_index:i+1,delay_minutes:i===0?0:Number(s.delay_minutes||0),message:s.message||'',image_data_url:s.image_data_url||null,image_name:s.image_name||null,cta_enabled:s.cta_enabled!==false})})}await createDeliveries(c);await patch('rds_campaigns',c.id,{status:'AGENDADA'});res.json({ok:true,id:c.id})}catch(e){res.status(400).json({error:e.message})}});
app.post('/api/campaigns/:id/activate',async(req,res)=>{try{await patch('rds_campaigns',req.params.id,{status:'AGENDADA'});res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.delete('/api/campaigns/:id',async(req,res)=>{try{await sb(`/rest/v1/rds_campaigns?id=eq.${q(req.params.id)}`,{method:'DELETE'});res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/process',async(req,res)=>{try{res.json({ok:true,processed:await processDue()})}catch(e){res.status(500).json({error:e.message})}});

app.get('/api/orders',async(req,res)=>{try{res.json({items:await sb('/rest/v1/rds_orders?select=*&order=updated_at.desc')})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/orders/:id/confirm-payment',async(req,res)=>{try{await patch('rds_orders',req.params.id,{status:'PAGAMENTO_CONFIRMADO',payment_confirmed_at:now()});res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/orders/:id/reject-proof',async(req,res)=>{try{const o=(await sb(`/rest/v1/rds_orders?id=eq.${q(req.params.id)}&select=*&limit=1`))[0];await patch('rds_orders',o.id,{status:'AGUARDANDO_PAGAMENTO',proof_type:null,proof_received_at:null});if(connected)await sendText(o.phone,`❌ O comprovante do pedido ${o.code} não foi confirmado. Verifique e envie novamente.`);res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.post('/api/orders/:id/tickets-sent',async(req,res)=>{try{const o=(await sb(`/rest/v1/rds_orders?id=eq.${q(req.params.id)}&select=*&limit=1`))[0],s=await settings();await patch('rds_orders',o.id,{status:'COMPRA_CONCLUIDA',tickets_sent_at:now(),completed_at:now()});if(connected)await sendText(o.phone,`${s.final_message}\nPedido: ${o.code}`);res.json({ok:true})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/returns',async(req,res)=>{try{const os=await sb('/rest/v1/rds_orders?status=in.(COLETANDO_DADOS,AGUARDANDO_PAGAMENTO,COMPROVANTE_RECEBIDO,PAGAMENTO_CONFIRMADO)&select=*&order=updated_at.desc');const items=[];for(const o of os){const c=o.contact_id?(await sb(`/rest/v1/rds_contacts?id=eq.${q(o.contact_id)}&select=name&limit=1`))[0]:null;const m=(await sb(`/rest/v1/rds_messages?phone=eq.${q(o.phone)}&direction=eq.IN&select=body,created_at&order=created_at.desc&limit=1`))[0];items.push({name:c?.name||o.customer_name,phone:o.phone,stage:o.status,last_message:m?.body||'',updated_at:o.updated_at})}res.json({items})}catch(e){res.status(500).json({error:e.message})}});
app.get('/api/summary',async(req,res)=>{try{const [c,ca,d,o]=await Promise.all([sb('/rest/v1/rds_contacts?select=id'),sb('/rest/v1/rds_campaigns?select=id,status'),sb('/rest/v1/rds_deliveries?status=eq.AGENDADA&select=id'),sb('/rest/v1/rds_orders?select=id,status')]);res.json({contacts:c.length,campaigns:ca.length,scheduled:d.length,returns:o.filter(x=>['COLETANDO_DADOS','AGUARDANDO_PAGAMENTO','COMPROVANTE_RECEBIDO','PAGAMENTO_CONFIRMADO'].includes(x.status)).length,orders:o.length,proofs:o.filter(x=>x.status==='COMPROVANTE_RECEBIDO').length})}catch(e){res.status(500).json({error:e.message})}});
app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'index.html')));
app.listen(PORT,async()=>{console.log(`CANAL DE VENDAS RDS V6.0 na porta ${PORT}`);try{await startWA(false)}catch(e){console.log('WhatsApp aguardando:',e.message)}});
