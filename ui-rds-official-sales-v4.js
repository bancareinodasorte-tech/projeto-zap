(()=>{
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const clean=v=>String(v??'').trim();
const json=async(u,o={})=>{const r=await fetch(u,{cache:'no-store',...o});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||d.error||'Falha na operação.');return d;};
async function card(){const app=document.querySelector('#app');if(!app||!app.innerHTML.includes('<h1>Ajustes</h1>'))return;document.getElementById('rdsOfficialSalesCard')?.remove();const old=document.getElementById('rdsIssuerCard');if(old)old.style.display='none';const c=document.createElement('div');c.id='rdsOfficialSalesCard';c.className='card rds-issuer-card';app.appendChild(c);c.innerHTML='<span class="eyebrow">INTEGRAÇÃO OFICIAL V4</span><h2>Sistema de vendas REINO DA SORTE</h2><p class="mut">Conexão direta com o sistema oficial. Sessão própria do servidor.</p><div id="r4state" class="priority"><strong>Verificando conexão...</strong></div><div id="r4dev" class="priority"><strong>Dispositivo do servidor</strong></div><div id="r4draw" class="priority"><strong>Sorteio oficial</strong></div><div id="r4act" class="row"></div>';
try{const b=await json('/api/v1012/official-sales/bootstrap');document.getElementById('r4dev').innerHTML='<strong>🖥️ Dispositivo do servidor</strong><p class="mut" style="word-break:break-all">'+esc(b.deviceId||'—')+'</p><p class="mut">Identificador exclusivo do CANAL DE VENDAS.</p>';const s=document.getElementById('r4state');if(b.emailConfigured&&!b.authorized)s.innerHTML='<strong>🟠 Servidor aguardando autorização</strong><p class="mut">O servidor já possui as credenciais. Autorize este dispositivo no sistema administrativo do REINO DA SORTE.</p>';else if(b.authorized)s.innerHTML='<strong>🟢 Dispositivo oficial autorizado</strong><p class="mut">Sessão persistida com proteção no servidor.</p>';else if(!b.emailConfigured)s.innerHTML='<strong>🔴 Credenciais não disponíveis no runtime</strong><p class="mut">O servidor não está recebendo as credenciais oficiais neste endpoint.</p>';document.getElementById('r4act').innerHTML=b.emailConfigured&&!b.authorized?'<button class="btn primary" onclick="rdsOfficialAuthorizeDevice()">Autorizar este dispositivo</button>':'<button class="btn primary" onclick="rdsOfficialRefreshSettings()">Atualizar conexão</button>';if(b.authorized){try{const st=await json('/api/v1012/official-sales/status');s.innerHTML='<strong>🟢 Vendedor oficial conectado</strong><p class="mut">'+esc(st.seller?.name||st.seller?.email||'CANAL DE VENDAS RDS')+'</p>';}catch(e){s.innerHTML='<strong>🟠 Sessão precisa ser renovada</strong><p class="mut">'+esc(e.message)+'</p>';}}try{const d=await json('/api/v1012/official-sales/draw-info');const x=d.data||{};document.getElementById('r4draw').innerHTML='<strong>🟢 Sorteio oficial disponível</strong><p class="mut">'+esc(x.drawTitle||x.title||x.name||'Sorteio ativo')+' • ID '+esc(x.drawId||'—')+' • '+money(x.pricePerTicket)+' • '+esc(x.totalBooklets??x.availableBooklets??'—')+' bloco(s) disponíveis</p>';}catch(e){document.getElementById('r4draw').innerHTML='<strong>🟠 Sorteio ainda não consultado</strong><p class="mut">'+esc(e.message)+'</p>';}}
catch(e){document.getElementById('r4state').innerHTML='<strong>🔴 Falha na integração</strong><p class="mut">'+esc(e.message)+'</p>';}}
window.rdsOfficialAuthorizeDevice=()=>{const m=document.createElement('div');m.className='modal';m.innerHTML='<div><div class="row" style="justify-content:flex-end"><button class="btn" onclick="this.closest(\'.modal\').remove()">✕</button></div><span class="eyebrow">AUTORIZAÇÃO DO SERVIDOR</span><h2>Autorizar dispositivo</h2><p>Digite o código gerado pelo administrador do sistema oficial.</p><input id="r4code" class="input-field" autocomplete="off" placeholder="Código de autorização"><div class="row" style="margin-top:12px"><button class="btn primary" onclick="rdsOfficialSubmitAuthorization()">Autorizar</button></div><p id="r4msg" class="mut"></p></div>';document.body.appendChild(m);document.getElementById('r4code')?.focus();};
window.rdsOfficialSubmitAuthorization=async()=>{const code=clean(document.getElementById('r4code')?.value),msg=document.getElementById('r4msg');if(!code){msg.textContent='Digite o código de autorização.';return;}msg.textContent='Autorizando dispositivo...';try{await json('/api/v1012/official-sales/authorize',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({authorizationCode:code})});document.querySelector('.modal')?.remove();alert('🟢 Dispositivo oficial autorizado com sucesso.');card();}catch(e){msg.textContent=e.message||'Código inválido ou expirado.';}};
window.rdsOfficialRefreshSettings=()=>{document.getElementById('rdsOfficialSalesCard')?.remove();card();};
window.rdsEmitTickets=async id=>{try{const orders=await json('/api/orders');const o=(orders||[]).find(x=>x.id===id);if(!o)throw new Error('Pedido não encontrado.');const customerName=clean(o.customer_name||o.name||''),customerPhone=clean(o.phone||o.contact_phone||o.customer_phone||''),quantity=Math.max(1,Number(o.quantity||1));if(!customerName||!customerPhone)throw new Error('O pedido não possui nome e telefone completos.');const r=await json('/api/v1012/official-sales/issue',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerName,customerPhone,quantityBooklets:quantity,paymentMethod:'pix',lotNumber:1})});alert('🟢 Emissão oficial concluída. Venda '+String(r.data?.saleId||'')+'.');if(typeof render==='function')await render();return r;}catch(e){alert(e.message||'Falha na emissão oficial.');throw e;}};
function boot(){card();const app=document.getElementById('app');if(!app)return;let q=false;new MutationObserver(()=>{if(q)return;q=true;setTimeout(()=>{q=false;card();},150);}).observe(app,{childList:true,subtree:true});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();